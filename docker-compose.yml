version: '3.8'

services:
  web:
    image: omkarpathak27/trackmyrupee:latest
    container_name: django-finance-tracker_web_1
    command: gunicorn --workers 2 --threads 2 --max-requests 1000 --max-requests-jitter 50 --bind 0.0.0.0:8000 finance_tracker.wsgi:application
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 400M
        reservations:
          memory: 128M
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - proxy-network
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  proxy-network:
    external: true
